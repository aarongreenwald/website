<div>
    <div>
        <div class="blog-title">
          <a href="/blog">My Coding Blog</a>
        </div>
        <div class="blog-tagline">because there weren't enough programming blogs out there yet</div>
         <div class="content">
            <% if (showingTag) { %>
                <div class="tag-notice">
                    Showing posts tagged <span class="tag"><%=showingTag%></span> <a class="pull-right" href="/blog">view all</a>
                </div>
            <% } %>


            <% if (!posts) { %>
                <div class="post">
                    <div class="post-title"><%=post.title%></div>
                    <div class="post-date"><%=post.formattedDate%></div>
                    <div class="post-content"><%-post.content%></div>
                    <div style="margin-bottom:30px;" class="tags">
                        <% post.tags.forEach(tag => { %>
                            <a href="/blog/tag/<%=tag%>" title="View posts tagged '<%=tag%>'"><%=tag%></a>
                        <% }) %>
                    </div>
         	    </div>

            <% } %>

            <% posts && posts.forEach(post => {%>
                <div class="post post-list">
                    <div class="post-title"><a href="/blog/<%=post.slug%>"><%=post.title%></a></div>
                    <div class="post-date"><%=post.formattedDate%></div>
                </div>
            <% }) %>

            <% if (!posts) { %>
                <div style="margin:80px 0;width: 100%;">
                  Questions? Comments? Rants? A blog is not a blog without a place readers can use
                  to tell the world how much money they make by just clicking links on Google. Head to
                  Twitter to join the discussion. I'm <a href="//twitter.com/aaronjgreenwald">@aaronjgreenwald</a> and I'd love to hear from you.
                </div>
            <% } %>


         </div>
         <div class="sidebar">
            <div class="about">
              You're reading Aaron Greenwald's coding blog,
              where he writes infrequently about code, software and programming.
              Read more about him (me, actually) <a href="/">here</a>.
            </div>
            <% if (posts && tags) { %>
              <div class="tags">
                <div>
                  Tags
                </div>
                <%
                    tags.forEach(tag => { %>
                    <a href="/blog/tag/<%=tag%>" title="View posts tagged '<%=tag%>'"><%=tag%></a>
                <% }) %>
              </div>
            <% } %>
         </div>
    </div>
</div>
<style>


.blog-title {
  font-size:3em;
  line-height: 1em;
}

.blog-title a,
.blog-title a:hover {
  color:black;
  text-decoration:none;
}

.blog-tagline {
  font-style:italic;
  font-size: .8em;
  margin-top: 15px;
}

.sidebar{
  display: inline-block;
  margin-top: 20px;
}

#content {
  max-width: 60em;
}


@media only screen and (min-width:1200px){

  .sidebar{
    float:right;
    width:200px;
  }

  .content {
    float:left;
    width:calc(100% - 200px);
    padding-right:100px;
  }
}

.content .post {
  margin-top:2em;
}

.content .post .post-title {
  font-size:2em;
  line-height: 1.3em;
  margin-right:10px;
}

.content .post:not(.post-list) .post-date {
  font-size: 1.2em;
  margin-top:5px;
  font-style: italic;
  margin-bottom:15px;
}

.content .post .post-content {
  display:inline-block;
  line-height: 1.6em;
  font-size: 1.2em;
}

.content .post .post-content span.img-caption {
  font-style: italic;
  font-size: .8em;
  display: table;
  margin-left: auto;
  margin-right: auto;
}

.content .post-list.post {
  margin-top: 1em;
}

.content .post-list .post-title {
  font-size: 1.15em;
}

.content .post-list .post-date {
  font-size: .9em;
}

.content .tag-notice {
  padding:5px 3px;
  margin-top:10px;
  border:1px solid #ddd;
  background-color:#eee;
  border-radius:3px;
}

.content .tag-notice .tag {
  font-style:italic;
  font-size:12pt;
}

.tags {
  float:right;
}

.tags a {
  color:#666;
  font-style:italic;
  text-decoration:underline;
  margin-left:5px;
}

pre {
  margin: 30px 0;
}

.post-content img {
  margin-left: auto;
  margin-right: auto;
  display: flex;
}

.sidebar .about {
  font-style:italic;
  line-height: 1.5em;
}

.sidebar .tags {
  margin-top: 1em;
  float: none;
}

.sidebar .tags a {
  margin-top: 1em;
  float: none;
  margin-left: 0;
  margin-right: 5px;
}

pre {
    overflow-x: scroll;
    max-width: 97vw;
}

code {
    background-color: rgba(100, 100, 100, 0.25);
    border-radius: 4px;
    padding: 2px 5px;
}

@media only screen and (min-width:1100px){
  pre {
    max-width: 700px;
  }
}


</style>

<link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.12.0/styles/zenburn.min.css">
<script src="//cdnjs.cloudflare.com/ajax/libs/highlight.js/9.12.0/highlight.min.js"></script>
<script>hljs.initHighlightingOnLoad();</script>
